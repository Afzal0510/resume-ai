{
  "name": "resume-ai-root",
  "version": "1.0.0",
  "scripts": {
    "install-backend": "cd backend && npm install",
    "install-frontend": "cd frontend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "prepare-public": "rm -rf public && mkdir public && cp -r frontend/build/* public/",
    "build": "npm run install-backend && npm run install-frontend && npm run build-frontend && npm run prepare-public",
    "start": "cd backend && npm start"
  }
}
